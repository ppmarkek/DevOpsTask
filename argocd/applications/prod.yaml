apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: wp-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/ppmarkek/DevOpsTask.git
    targetRevision: main
    path: helm/wordpress
    helm:
      valueFiles:
        - values-prod.yaml
      parameters:
        - name: image.repository
          value: ghcr.io/ppmarkek/devopstask/wordpress
        - name: image.tag
          value: main
        - name: image.pullPolicy
          value: Always
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true

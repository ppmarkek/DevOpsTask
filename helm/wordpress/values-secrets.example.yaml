# Optional local overrides for secrets (copy to values-secrets.yaml — gitignored).
# Used when you prefer Helm-managed secrets instead of bootstrap-secrets.ps1.

standaloneMariadb:
  existingSecret: ""
  rootPassword: "change-me-root"
  password: "change-me-wp"

wordpress:
  existingSecret: ""
